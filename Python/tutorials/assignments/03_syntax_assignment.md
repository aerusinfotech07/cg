# Assignment: Python Syntax

Practice syntax, indentation, variables, and comments.

---

## Exercise 1 (Easy)
Write a correctly indented `if` block that prints "Positive" when `x` is greater than 0. Use `x = 5`.

**Expected Output:**
```
Positive
```

---

## Exercise 2 (Easy)
Add a comment above a print statement explaining what it does. The comment should say "Prints a greeting".

---

## Exercise 3 (Easy)
Create two variables: `a = 10` and `b = "Python"`. Print both with their types using `type()`.

**Expected Output:**
```
10 <class 'int'>
Python <class 'str'>
```

---

## Exercise 4 (Medium)
Write an `if-else` block. If `score >= 60`, print "Pass"; otherwise print "Fail". Use `score = 75`.

**Expected Output:**
```
Pass
```

---

## Exercise 5 (Medium)
Fix this code (it has an indentation error):
```python
if 3 > 2:
print("Three is greater")
```

**Corrected:**
```python
if 3 > 2:
    print("Three is greater")
```

---

## Exercise 6 (Hard)
Write a program with a nested `if`: if `x > 0`, check if `x` is even; if even, print "Positive even", else print "Positive odd". Use `x = 4`.

**Expected Output:**
```
Positive even
```
